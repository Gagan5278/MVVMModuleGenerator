//
//  ___FILEHEADER___
//

import UIKit

class ___VARIABLE_productName:identifier___Model: Decodable {
    
    //EXAMPLE USE CASE
    /*
     let userNameString: String
     let passwordString: String
     let imageURLString: String
     let ageInt: Int
    
    enum CodingKeys: String, CodingKey {
        case userName = "userName"
        case password = "password"
        case imageURL = "imageURL"
        case age = "age"
    }
    
    required init(from decoder: Decoder) throws {
        let encoder = try decoder.container(keyedBy: CodingKeys.self)
        self.userNameString = try encoder.decode(String.self, forKey: .userName)
        self.passwordString = try encoder.decode(String.self, forKey: .password)
        self.imageURLString = try encoder.decode(String.self, forKey: .imageURL)
        self.ageInt = try encoder.decode(Int.self, forKey: .age)
    }
     
     */
}
